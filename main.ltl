// ltl-kmer-count/main.ltl
//
// K-mer frequency counter with Bloom pre-filter.
//
// This is an early skeleton — contribution is welcome! See the
// README for build instructions.

import std::io
import std::fmt

pub fn main(args: [str]) -> Int {
    io::println("ltl-kmer-count v0.1.0")
    io::println("K-mer frequency counter with Bloom pre-filter.")
    0
}
