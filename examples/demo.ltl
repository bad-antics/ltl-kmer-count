// examples/demo.ltl — runnable demo for ltl_kmer_count

import std::io
use ltl_kmer_count as lib

fn main() {
    io::println("ltl_kmer_count demo starting...")
    let result = lib::main() |> io::inspect()
    io::println("demo complete")
}